type Query {
    user( id:ID! ): User
    users: [User]
}
# import User from "./schemas/user.graphql"

type Query {
    user( id:ID! ): User
    users: [User]
}

type Mutation {
    createUser( user: UserInput! ) : User
}